{
  "name": "@search/root",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "core",
    "mcp"
  ],
  "scripts": {
    "build": "bun run --cwd core build && bun run --cwd mcp build",
    "build:all": "bun run --cwd core build && bun run --cwd mcp build && bun run --cwd frontend build",
    "check": "bun run --cwd core check && bun run --cwd mcp check && bun run --cwd frontend check",
    "test": "bun test",
    "dbmate:new": "docker compose run --rm search-dbmate new",
    "dbmate:up": "docker compose run --rm search-dbmate up",
    "dbmate:down": "docker compose run --rm search-dbmate rollback"
  }
}

